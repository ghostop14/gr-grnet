# auto-generated by grc.converter

id: grnet_ByteComplexDecimator
label: Byte Complex Stream Decimator
category: '[GrNet Networking Tools]'

parameters:
-   id: decimation
    label: Decimation
    dtype: int
    default: '1'

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: import grnet
    make: grnet.ByteComplexDecimator(${decimation})

documentation: |-
    This block will directly decimate an incoming byte stream made up of 8-bit complex samples.  For instance if you have a source streaming 8-bit complex at high speeds and you want to decimate directly before writing to a file or network sink.

file_format: 1
