<?xml version="1.0"?>
<block>
  <name>Interleaved Short to Complex</name>
  <key>grnet_SC16ToComplex</key>
  <category>[GrNet Networking Tools]/Type Converters</category>
  <import>import grnet</import>
  <make>grnet.SC16ToComplex()</make>
  <sink>
    <name>in</name>
    <type>short</type>
  </sink>
  <source>
    <name>out</name>
    <type>complex</type>
  </source>
  <doc>
  This block incorporates an SHRT_MAX scaling factor into the conversion similar to csdr type conversions.
  
  For comparison:
  IChar to Complex uses a out = (float)in
  This block converts using out = (float)in / (float)SHRT_MAX
  </doc>
</block>
