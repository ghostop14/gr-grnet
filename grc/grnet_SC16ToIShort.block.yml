# auto-generated by grc.converter

id: grnet_SC16ToIShort
label: Complex Int16 to Interleaved Short
category: '[GrNet Networking Tools]/Type Converters'

inputs:
-   domain: stream
    dtype: sc16

outputs:
-   domain: stream
    dtype: short

templates:
    imports: import grnet
    make: grnet.SC16ToIShort()

documentation: |-
    This block converts complex int16 such as that out of a USRP source in 'complex int16' mode to interleaved shorts.

file_format: 1
