<?xml version="1.0"?>
<block>
  <name>Scaled Signed8 To Complex</name>
  <key>grnet_Signed8ToComplex</key>
  <category>[GrNet Networking Tools]</category>
  <import>import grnet</import>
  <make>grnet.Signed8ToComplex()</make>
  <sink>
    <name>in</name>
    <type>byte</type>
  </sink>
  <source>
    <name>out</name>
    <type>complex</type>
  </source>
  <doc>
  This block incorporates an SCHAR_MAX scaling factor into the conversion, making the output a -1 to 1 scale.
  
  For comparison:
  IChar to Complex uses a out = (float)in
  This block converts using out = (float)in / (float)SCHAR_MAX
  </doc>
</block>
