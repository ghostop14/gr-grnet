# auto-generated by grc.converter

id: grnet_FifoBuffer
label: FIFO Buffer
category: '[GrNet Networking Tools]'

parameters:
-   id: type
    label: Input Type
    dtype: enum
    options: [complex, float]
    option_attributes:
        size: [gr.sizeof_gr_complex, gr.sizeof_float]
    hide: part
-   id: minQueueLength
    label: Init Min Size
    dtype: int
    default: '24000'

inputs:
-   domain: stream
    dtype: ${ type }

outputs:
-   domain: stream
    dtype: ${ type }

templates:
    imports: import grnet
    make: grnet.FifoBuffer(${type.size}, ${minQueueLength})

documentation: "This block provides some initial buffering before starting to transmit\
    \ output data.  This can be helpful for instance with an audio sink where underruns\
    \ can cause issues.  This block can ensure there's enough data by queueing up\
    \ some samples ahead of time before starting to transmit.\n\t\n\tNOTE: The buffer\
    \ will output zeros until enough data is available."

file_format: 1
